/***
	SpellingBee.js 3.0
	
	**No copyright infringement is intended. No mirror image of this library should be made without creator's consent.
	**This is not expected to work as intended because this is my first library, so let me know if you have problems so I can update the library.
***/
(function(window, undefined) {
	var $spelling_bee = function(words) {
		// Instanciate and utilize our library
		if(window === this) {
			return new $spelling_bee(words);
		}
		
		var spellingBee = {};
		spellingBee.words = words;
		spellingBee.checkArray = function(object) {
			if(!(Array.isArray(object))) {
				throw new Error("Argument is of wrong type.");
			}
		};
		spellingBee.addDefinitions = function(definitions) {
			spellingBee.checkArray(definitions);
			spellingBee.definitions = definitions;
		};
		spellingBee.createSpellingBee = function() {
			spellingBee.checkArray(words);
			spellingBee.addDefinitions();
			
			var spellingArea = document.createElement("form");
			spellingArea.setAttribute("action", "#");
			spellingArea.setAttribute("name", "spelling-bee");
			spellingArea.setAttribute("method", "POST");
			
			for(var j = 0; j < definitions.length; j++) {
				for(var i = 0; i < words.length; i++) {
					var label = document.createElement("label");
					var text = document.createTextNode(definitions[j] "<br />");
					label.appendChild(text);
					spellingArea.appendChild(label);
					
					var form = document.createElement("input");
					form.setAttribute("type", "text");
					form.setAttribute("name", "spelling-area");
					spellingArea.appendChild(form);
					
					var check = document.createElement("input");
					check.setAttribute("type", "submit");
					check.setAttribute("value", "Check");
					spellingArea.appendChild(check);
				}
			}
			document.body.appendChild(spellingArea);
		}
		return spellingBee;
		
		return this;
	};
	window.$spelling_bee = $spelling_bee;
})(window);
